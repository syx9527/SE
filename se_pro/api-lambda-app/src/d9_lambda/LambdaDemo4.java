package d9_lambda;

/**
 * Lambda表达式的省略写法（进一步在Lambda表达式的基础上继续简化）
 * - 参数类型可以省略不写
 * - 如果只有一个参数，参数类型可以省略，同时()也可以省略
 * - 如果Lambda表达式的方法体只有一行代码，可以省略大括号不写，同时还要省略分号
 * - 如果Lambda表达式的方法体代码只有一行代码，可以省略大括号不写。此时，如果这行代码时return语句，必须省略return不同，同时也必须省略分号
 *
 * @author SYX
 */
public class LambdaDemo4 {
    public static void main(String[] args) {

    }
}
